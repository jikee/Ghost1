<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.mph.ghost.ghost1"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".utils.TheApplication"
        android:allowBackup="true"
        android:icon="@drawable/launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.BaseActivity"/>
        <activity android:name=".activities.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.NativeMainActivity">
        </activity>
        <activity android:name=".activities.WebMainActivity">
        </activity>
        <activity android:name=".activities.JumpWebShow"/>
        <activity android:name=".activities.SSBTActivity"/>
        <activity android:name=".activities.LTBTActivity"/>
        <activity android:name=".activities.DCKJActivity"/>
        <activity android:name=".activities.SettingActivity">
        </activity>
    </application>

</manifest>